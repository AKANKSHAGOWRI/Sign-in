import 'package:flutter/material.dart';

class SubmittedFormsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Submitted Forms'),
      ),
      body: Center(
        child: Text('Display the submitted forms and CVs here'),
      ),
    );
  }
}
